apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: orderflow

resources:
  - namespace.yaml
  - ../../base

# CI bumps these tags after a successful build + scan + push (see
# .github/workflows/ci-cd.yml) — this file is the GitOps handoff point
# between CI and ArgoCD.
images:
  - name: ghcr.io/bims-creator/orderflow-api-service
    newTag: db856adb18ab5c60ed514b038859205d57cd0d42
  - name: ghcr.io/bims-creator/orderflow-worker-service
    newTag: db856adb18ab5c60ed514b038859205d57cd0d42
